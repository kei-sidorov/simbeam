# Лог решений (ADR-lite)

Короткие записи принятых решений. Новое решение — новая строка. Если решение меняем —
не удаляем старое, а добавляем новое с пометкой «отменяет №…».

| # | Решение | Почему |
|---|---------|--------|
| 1 | Скоуп — только симуляторы, не реальные устройства | резко упрощает; idb силён на симуляторах |
| 2 | Сидим на `idb_companion`, не пишем свой движок видео/ввода | готовое, нативное, работает; не переписываем работающее |
| 3 | Говорим gRPC напрямую с companion, `fb-idb` (python) не используем | убираем питон-прослойку и двойной хоп |
| 4 | Демон на **Go** | после делегирования видео в idb демон = сетевой перекладчик байтов |
| 5 | WebRTC-сервер на **pion** | лучший серверный WebRTC; отдаёт готовый H.264 без энкодинга |
| 6 | Клиент на **Swift + GoogleWebRTC** | приёмная сторона WebRTC на iOS — проторённый путь |
| 7 | MVP без NAT-traversal/TURN; своё E2E НЕ пишем | WebRTC уже даёт DTLS-SRTP; relay нужен только для удалёнки позже |
| 8 | Дистрибуция: GoReleaser + Homebrew tap, prebuilt бинари | без нотаризации (brew не quarantine), `depends_on idb-companion` |
| 9 | Open-core: сервер OSS, клиент платный | ров — в UX клиента и облачной инфре, не в доступе к серверу |
| 10 | Первый прототип — JPEG-over-WS, WebRTC вторым шагом | увидеть результат за вечер, не утонуть в WebRTC сразу |
| 11 | Module path `github.com/kei-sidorov/simcast` | по схеме `github.com/<user>/simcast`, выбрано пользователем |
| 12 | `internal/companion` парсит вывод `--list 1` построчно, беря только строки-JSON с `udid` | companion мешает JSON-строки девайсов с диагностикой на stdout; objc-warning на stderr — парсим устойчиво к мусору |
| 13 | Фильтруем `type != "Simulator"` в `List()` | скоуп — только симуляторы; companion заодно перечисляет подключённые реальные устройства |
| 14 | Версию берём из `--version` (JSON `build_time`/`build_date`), не из brew | единственный источник версии у самого бинаря; brew-версия (1.1.8) — это пакет, не отчёт companion |
| 15 | Phase 1: кадры через `video_stream(format=MJPEG)`, не screenshot-поллинг | companion сам отдаёт поток JPEG — ровно «JPEG over WS», без транскодинга, реальный fps; структурно ближе к Phase 2 |
| 16 | Phase 1: транспорт = REST (list/boot) + 1 WS на сессию (кадры↓ binary, тапы↑ JSON) | разные природы взаимодействий; WS full-duplex закрывает стрим+ввод одним соединением |
| 17 | simcastd спавнит companion-сайдкар по требованию (на WS-коннект), бут — отдельной командой | вертикальный срез одной кнопкой; бут управляется клиентом (список+boot), стрим только к booted |
| 18 | Пакеты: `companion` (CLI lifecycle) отдельно от `idb` (gRPC describe/video/hid) | две природы companion'а (CLI-флаги vs gRPC-сервер) — разные границы |
| 19 | Сгенерённые gRPC-стабы (`internal/idbpb`) коммитим в git; команды через Makefile | сборка у потребителя без protoc; виден diff стабов |
| 20 | Координаты тапа — нормализованные `[0,1]` от клиента, сервер масштабирует по `describe` | resolution-independent, не зависит от scale_factor кадра; px-vs-points решаем живым тапом |
| 21 | Debug-клиент — отдельный файл, раздаётся только при `--web`; бинарь самодостаточен по API | клиент только для дебага, не поставляется; реальные клиенты подключатся к REST+WS |
| 22 | **Отменяет №15.** Кадры Phase 1 — поллинг `screenshot` (~10/сек, PNG), не `video_stream` MJPEG | проверено на companion 1.1.8: MJPEG отдаёт 1 кадр на статичном экране и замирает; H264 непрерывен, но не рисуется в `<img>`. `idb screenshot`-поллинг — то, что реально давало JPEG в ios-bridge. H264-видео уходит в Phase 2 (WebRTC). См. issue facebook/idb#846 |
| 23 | Размер кадра (PNG ~2.3 МБ @ 10fps) пока не оптимизируем | для локального MVP приемлемо; single-slot буфер сам дропает лишнее. Даунскейл/JPEG-перекодирование — позже, по необходимости |
| 24 | **Резолюция №20.** `hid` ждёт логические ТОЧКИ, не пиксели — `ScaleTap` масштабирует по `WidthPoints/HeightPoints` | проверено живьём: тап в пиксельных координатах (центр → px 603×1311) уходил за пределы экрана в точках (402×874) → тача нет. Перешли на точки → центр-тап (201×437) попадает |
| 25 | Свайпы и клавиатура втянуты в Phase 1 (свайп = `HIDSwipe{start,end,duration}`; клавиши = USB HID usage + shift) | по запросу; фундамент `hid` уже был. Phase 2 остаётся «только H264» |
| 26 | Клавиатура шлёт физические HID-коды; символ выбирает раскладка симулятора (известное ограничение, не баг) | HID = scancodes; iOS применяет активную аппаратную раскладку сима. Раскладко-независимый ввод (pbcopy+paste) — отложено, вне скоупа Phase 1 |
| 27 | Phase 2: клиент = браузерный debug-клиент (`<video>` + WebRTC), нативный iPad остаётся Phase 3 | браузер нативно принимает H.264 по WebRTC; самый дешёвый способ увидеть DoD фазы, не таща вперёд Phase 3 |
| 28 | Phase 2: WebRTC и JPEG/WS (`/session`) сосуществуют — WebRTC основной, JPEG fallback/debug | страховка на случай капризов H.264 (ср. №22); Phase 1 не трогаем |
| 29 | Phase 2: спайк — шаг 0. Доказать `idb video_stream H264 → pion → <video>` до постройки сигналинга/DataChannel | главный риск фазы; история MJPEG (№22) обязывает де-рискнуть пайплайн в начале, а не в конце |
| 30 | Phase 2: подход A — пакет `internal/rtc` (вся pion-логика) + WS-эндпоинт `/rtc`; команды ввода НЕ дублируем, переиспользуем `parseControl`/`ScaleTap`/`hid` | чистые границы: pion не знает про роутинг, ввод резолвится одним кодом для WS и DataChannel |
| 31 | Phase 2: тачи — по WebRTC DataChannel (unreliable/unordered), не по WS | инкрементально дёшево (peer уже поднят ради видео); по роадмапу; один P2P-линк, готовность к Phase 4 (TURN) |
| 32 | Phase 2: сигналинг — один WS-заход offer→answer, non-trickle, браузер-инициатор; только host-кандидаты, без STUN/TURN | на localhost сбор ICE мгновенный, trickle избыточен; STUN/TURN — Phase 4 (№7) |
| 33 | Phase 2: H.264-ингест = `idb.VideoStream(ctx, fps)` рядом с `ScreenshotStream`, отдаёт access unit'ы; fps старт 30 | симметрия с Phase 1; H.264-нарезка изолирована в `internal/idb`, не знает про WebRTC. fps/SPS-PPS/PTS/профиль финализирует спайк |
| 34 | **Находка спайка, отменяет №33 и passthrough-замысел Phase 2.** Сырой H.264 из `video_stream` НЕ годится для интерактива | замерено живьём (companion 1.1.8): idb шлёт IDR с фиксированным GOP ~10с и НЕ вставляет keyframe на смену сцены; в gRPC нет управления GOP/keyframe (только fps/format/compression/scale). На резкой смене сцены — артефакты до ~10с. ios-bridge ту же проблему не решил (прод = JPEG, H.264-прототип декодил+переэнкодил). См. память `idb-h264-keyframe-limit` |
| 35 | **Phase 2 видеопайплайн:** idb `screenshot` (PNG-поллинг, путь Phase 1) → `ffmpeg` (h264_videotoolbox, короткий GOP) → pion. Мы владеем энкодером → владеем keyframe'ами | источник кадров чистый (каждый screenshot самодостаточен), keyframe раз в ~1–2с под нашим контролем, артефактов на смене сцены нет. Это прод-подход ios-bridge. NAL-сплиттер/сборка AU из спайка переживают — кормятся выводом ffmpeg, а не idb |
| 36 | Phase 2: `ffmpeg` — обязательная внешняя зависимость (подпроцесс, как idb; brew `depends_on ffmpeg`), аппаратный энкод `h264_videotoolbox` | idb по-прежнему захватывает (screenshot) — мы лишь ПЕРЕкодируем его вывод; это НЕ «свой ScreenCaptureKit-пайплайн» (тот остаётся отложен). H.264/videotoolbox: пуленепробиваемый декод на Safari/iPad + аппаратный энкод. VP9/AV1 screen-кодеки — Phase 4 (узкий канал удалёнки) |
| 37 | Phase 2: low-latency настройки. ffmpeg `-analyzeduration 0 -fflags nobuffer -flags low_delay`, выход `-flush_packets 1 -max_delay 0`; браузер `receiver.jitterBufferTarget=0` | `-analyzeduration 0` убрал стартовый бэклог демуксера (он держал константные ~3с). Замерено: 3с → ~0.7с @15fps localhost. Дальнейший тюнинг (fps↑, `-vf scale` ↓размер кадра/IDR, пейсинг) — отдельная задача |
| 38 | Phase 2: парсинг H.264 Annex-B. Сплиттер пропускает ПОЛНУЮ длину start-code (3 или 4 байта); граница access unit — ПЕРЕД ведущими SPS/PPS/SEI (типы 7/8/6) или AUD(9)/VCL | два бага из спайка: (1) сдвиг на 1 байт ловил «вложенный» 3-байтный код в 4-байтном → мусорный NAL; (2) флаш только на VCL отрывал SPS/PPS от их IDR → браузер не мог декодить keyframe. Фиксы обязательны в боевом коде |
| 39 | Phase 2: у RTP-пампа `/rtc` НЕТ жёсткого write-deadline (в отличие от `/session`, где 10с на кадр). Зависший клиент держит ffmpeg+сайдкар до ICE-таймаута pion (~5–30с), который через `OnConnectionStateChange`→`fireClose`→`cancel()` рвёт сессию | `track.WriteSample` не принимает deadline; вотчдог — лишняя сложность для localhost-скоупа (№32). Таймаут pion ограничивает удержание ресурсов по времени. Пересмотреть при привязке к сетевому интерфейсу (ср. комментарий в `session.go`) |
| 40 | Phase 2 тюнинг латентности (Task 9): добавили `-vf scale=iw/2:ih/2` в энкодер — кодируем H.264 в половинном разрешении, браузер апскейлит обратно в `<video>` | кадр/keyframe ~в 4× меньше → меньше энкод+транзит. Подтверждено живьём: латентность ниже базовых ~0.7с, текст сима остаётся читаемым на апскейле. fps оставили 15 (не трогали по выбору пользователя — только frame-size) |
| 41 | Phase 4 удалёнка — продукт для широкой аудитории: managed signaling + STUN/TURN + аккаунты как облачный сервис и ров open-core | подтверждено пользователем; совпадает с №9 (сервер OSS, клиент+облако платные). Дизайн — `docs/superpowers/specs/2026-06-03-phase4-remote-access-design.md` |
| 42 | Phase 4: рандеву (signaling) используется ВСЕГДА, даже в одной сети; одну сеть обслуживает сам ICE (host-кандидаты приоритетнее) | два пира за NAT не знают адресов без знакомства; ручной «мы рядом?»-логики не пишем — ICE сам выбирает прямой LAN-путь, не трогая STUN/TURN |
| 43 | Phase 4: при удалёнке весь control И API (list/boot/describe/тачи) уезжают в WebRTC DataChannel; демон делает только исходящее, ноль открытых портов на Mac | нет attack surface, работает из-за домашнего NAT без проброса портов; переиспользуем `parseControl`/`ScaleTap`/`hid` (№30), расширяя `/rtc` |
| 44 | Phase 4: TURN гейтится подпиской через короткоживущие HMAC-креды; free получает только STUN(+host), при `iceConnectionState=failed` — апселл «будьте в одной сети или платите» | TURN — единственный дорогой по трафику ресурс, гейт совпадает с себестоимостью 1:1; деградация мягкая (free работает по LAN и на дружелюбных NAT) |
| 45 | Phase 4: QR несёт `signalingURL` + одноразовый `pairingToken` + `daemonPubKey`; pubkey аутентифицирует сторону демона на сигналинге (анти-MITM) | флоу «как в happy» — QR несёт криптоидентичность, не адрес; медиа уже E2E через DTLS-SRTP (№7), защищаем только рукопожатие |
| 46 | Phase 4: control-плоскость и видео-плоскость разведены. PeerConnection + DataChannel поднимаются на пейринге (per-client-session, БЕЗ UDID) и живут всю сессию; видео-трек добавляется on-demand по команде `attach <udid>` (renegotiation, по возможности по самому DataChannel — signaling нужен только для первого рукопожатия) | за NAT list/boot должны работать ДО появления видео; DataChannel почти бесплатен (без сайдкара/ffmpeg). Отвязывает PeerConnection от UDID (ср. №17, №30): спавн сайдкара остаётся on-demand, триггер переезжает с «WS-connect с udid» на «DataChannel attach udid» |
| 47 | Phase 4: signaling/auth-сервер на **Go**, reference-версия в этом репо (`cmd/simcast-signal`); TURN — готовый `coturn`, свой TURN не пишем (только конфиг + выписка HMAC-кредов на Go) | один стек с демоном (№4), общие типы сообщений; сервер тонкий (WSS-брокер комнат + релей SDP/ICE + creds). Ров — хостинг managed-версии + TURN + аккаунты (№9), не секретность кода сигналинга |
| 48 | Phase 4: вся серверная удалёнка валидируется **браузерным** debug-клиентом (расширение Phase 2 `/rtc`, №27); нативный iPad-клиент — **отдельный репозиторий**, пишем только когда серверная часть здесь готова и проверена в браузере | браузер умеет весь нужный WebRTC (DataChannel, renegotiation, attach/detach); в браузере QR не нужен (token+pubkey через URL). Чёткая граница репозиториев и порядок работы по требованию пользователя |
| 49 | **Перестановка роадмапа.** Phase 3 теперь = удалёнка/рандеву (бывш. контент Phase 4); дистрибуция+прод-облако = Phase 4. Нативный iPad-клиент **вынесен из этого репо целиком** (отдельный репозиторий, потом, когда сервер готов) — больше не фаза здесь | нативный клиент отложен и изолирован (№48), значит следующая работа в этом репо — серверная удалёнка; держим роадмап в реальном порядке исполнения. Имя файла спеки (`phase4-…`) оставляем историческим |
| 50 | Phase 3a: видео-трек **pre-negotiated и молчит** до `attach` (Option B), без рантайм-renegotiation. PeerConnection поднимается per-session с control-DataChannel; `list`/`boot`/`attach`/`detach` + тачи — по DataChannel (двунаправленному); `attach <udid>` спавнит сайдкар+ffmpeg и пишет H.264 в существующий трек, `detach`/новый `attach` — останавливает | разрешает spike из №49: renegotiation не нужен — новый H.264-поток начинается с IDR (короткий GOP, №35), декодер ресинкается на смене сима. Проще и надёжнее. `/rtc` теперь без udid; JPG `/session` не трогаем (№28) |
| 51 | Phase 3b: сигналинг — **только рукопожатие**. Брокер релеит один offer→answer, затем сокет закрывается; пост-пейринговый обмен SDP отсутствует (видео-трек pre-negotiated, #50), renegotiation (если когда-нибудь понадобится) — по DataChannel, не по сигналингу. Disconnect ловит pion `OnConnectionStateChange` (#39) | разрешает «развилку» плана 3b: Option B (#50) убирает renegotiation вообще, поэтому signaling нужен только для знакомства и может закрыться сразу после connect — брокер остаётся по-настоящему тонким |
| 52 | Phase 3b: демон делает **исходящий** дозвон до брокера и остаётся **answerer** (браузер — offerer, как в 3a). Переиспользуем `rtc.Session.Answer` без изменений; non-trickle ICE (#32) сохраняется — кандидаты в SDP, работает и со STUN/TURN (медленнее сбор). Trickle ICE отложен | минимальная дельта к 3a: меняется только транспорт знакомства, механика сессии (`rtcDispatch`/attach) нетронута; роль answerer'а сохраняет переиспользование кода |
| 53 | Phase 3b: WSS-библиотека для брокера и исходящего дозвона демона — `gorilla/websocket` (уже зависимость репо), а не `coder/websocket` из спеки #47 | спека называла lib как ориентир, не как архитектурное решение; gorilla уже вендорится и используется (`/session`, `/rtc`), даёт и Upgrader, и Dialer — ноль новых зависимостей |
| 54 | Phase 3b: аутентификация рукопожатия — **Ed25519** (stdlib `crypto/ed25519`); демон подписывает answer-SDP сессионным ключом, браузер проверяет по `daemonPubKey` из URL-фрагмента (WebCrypto Ed25519). TURN-креды — HMAC-SHA1 REST (`username=<expiry>:<userID>`, #44). Гейтинг подписки — стаб `--grant-turn` (реальный биллинг — Phase 4) | асимметрия нужна против MITM на сигналинге (брокер не должен уметь подделать сторону демона); координаты в фрагменте URL не попадают в логи веб-сервера; медиа уже E2E (DTLS-SRTP, #7) |
| 55 | Phase 3C: авторизация связи — **peer-pinning**, брокер untrusted. Демон пиннит клиентские ключи, клиент пиннит `daemonPubKey`; доступ к подключению решают концы по ключам, не брокер | анти-MITM без доверия к рандеву; медиа уже E2E (DTLS-SRTP, #7), защищаем рукопожатие криптографией концов |
| 56 | Phase 3C: **постоянная идентичность демона** — долгоживущий Ed25519 на диске (`~/.simcast/identity.key`, 0600); `daemonID` = его pubkey. **Отменяет сессионный ключ из #54 и одноразовый token из #45/#51** | стабильный адрес на брокере + криптоудостоверение в одном; реконнект без повторного QR требует постоянного адреса |
| 57 | Phase 3C: **постоянное присутствие демона** — держит исходящий WSS к брокеру с auto-reconnect (экспон. backoff), всё время зарегистрирован под `daemonID`; presence у брокера в памяти `map[daemonID]→conn`. **Пересматривает #51** (signaling больше не «закрылся после рукопожатия») | Mac должен быть находим для реконнекта в любой момент; ноль открытых портов сохраняется (только исходящее) |
| 58 | Phase 3C: перед offer/answer — **взаимный challenge-response**. Демон challenge'ит клиента nonce'ом (клиент подписывает → демон проверяет владение ключом + pinned); брокер отдельным nonce'ом аутентифицирует клиентский ключ для гейта TURN; доказательство демона клиенту = **подписанный answer (#54)**, отдельный daemon-nonce не нужен | минимум сообщений: переиспользуем подпись answer'а как proof демона; брокер узнаёт проверенный `clientPubKey`, ничего не решая о доступе |
| 59 | Phase 3C: **первый пейринг — явное окно на демоне по нажатию `P` в терминале** (raw-mode, `golang.org/x/term`), одноразовый секрет `S` с TTL; клиент доказывает знание `S` через `HMAC-SHA256(S, clientPubKey‖0x00‖nonce)`, `S` брокеру не виден. Ревокация — `simcastd unpair <pubkey>` (локально) | интерактивный триггер по запросу пользователя; HMAC-доказательство анти-«чужой», окно single-use/TTL анти-абьюз |
| 60 | Phase 3C: БД минимальна — **одна таблица `subscriptions`** в SQLite за интерфейсом `Store`; ключи и список Mac'ов живут на концах (localStorage / iCloud Keychain), серверного восстановления нет | соответствует open-core: durable только то, что нужно для гейта; миграция на Postgres = вторая реализация `Store` |
| 61 | Phase 3C: SQLite-драйвер — **`modernc.org/sqlite`** (чистый Go, без cgo) | герметичные `go test` без C-тулчейна, тривиальная кросс-компиляция под Homebrew-дистрибуцию Phase 4 |
| 62 | Phase 3C: endpoint подписки `POST /v1/subscription` с **двумя подписями** — app-secret HMAC (слабый барьер «наш билд», честно обфускация) + Ed25519 account-подпись (настоящая привязка к ключу); idempotent last-write-wins по `issued_at`, время сравнения — серверные часы. Усиление чеком Apple — Phase 4, флип `source` без смены схемы | разделяем «наш билд» и «это тот аккаунт»; идемпотентность делает спам foreground/background безопасным |
| 63 | Phase 3C: **гейт TURN читает `Store`** по уже проверенному (challenge-response) `clientPubKey`; **стаб `--grant-turn` убран**. Решалки разведены: доступ к *подключению* — концы по ключам (#55); доступ к *TURN-реле* — брокер по подписке (его законная работа, он выдаёт `iceServers`) | гейт «бесплатен» на рукопожатии (один read, не горячий медиапуть); заменяет стаб реальной подпиской |
| 64 | Phase 3C: тест-стенд — **браузер играет iPad целиком**: WebCrypto Ed25519 аккаунт-ключ в `localStorage`, пейринг по фрагменту URL, «мои Mac'ы», панель эмуляции подписки (настоящий подписанный POST), индикатор TURN, авто-reconnect. `http://localhost` = secure context → весь WebCrypto/WS работает без TLS | валидация всего флоу до строчки Swift; протухание не эмулируем — ставим `expires_at` вперёд/назад |
| 65 | Phase 3C: **вытеснение сессии происходит до аутентификации нового клиента** (брокер закрывает WS старого клиента на `join`, демон делает `cleanup()` на `TypeConnect` до `pinned.Contains`/`win.verify`) — **принято как осознанный риск для self-host scope**. Кто знает `daemonID`, может рвать живую сессию (но не получить медиа: оно E2E DTLS-SRTP, рукопожатие не пройти). Обоснование: `daemonID` — это публичный ключ (не секрет); untrusted-брокер (#55) и так способен на DoS отказом релея; в self-host `daemonID` не утекает. Правильный фикс (pending-client challenge параллельно живой сессии, swap только при успехе, разведение nonce по клиентам) — это смена протокола, отложено | минимальный код сейчас важнее защиты от угрозы, которая в текущей модели (#55) либо уже доступна брокеру, либо требует утечки публичного адреса; не строим впрок (CLAUDE.md) |
| 66 | Phase 4: релизы — **GoReleaser, один пайплайн** на тег `v*` → два артефакта (`simcastd` darwin arm64/amd64, `simcast-signal` linux amd64), все pure-Go (`CGO_ENABLED=0`, sqlite = modernc, #61). Версия через ldflags `-X main.version` | один источник правды для сборки обоих бинарников; кросс-компиляция тривиальна без cgo |
| 67 | Phase 4: macOS-демон — **Homebrew tap `kei-sidorov/homebrew-simcast`, предсобранный неподписанный бинарь** (без Developer ID / нотаризации); зависимости `idb-companion` + `ffmpeg`. Обновление `brew upgrade` (без автообновления на Mac). Отгружается **cask** (а не formula): GoReleaser v2.16 жёстко выпилил `brews`, cask снимает quarantine post-install хуком `xattr -dr` | аудитория = iOS-разработчики, cask снимает quarantine → unsigned обычно запускается; подпись/нотаризацию добавим, если Gatekeeper прижмёт |
| 68 | Phase 4: автодеплой сервера — **pull-модель**: systemd timer на VPS тянет релиз из публичных GitHub Releases, проверяет checksum, атомарно ставит, рестартит юнит. **Ноль серверных секретов в репо и CI**; deploy-скаффолдинг (юниты, апдейтер, Caddyfile, bootstrap) генерик в репо, личные значения — в `/etc/simcast/signal.env` на сервере | прямая цель форкаемости: репо = образцовый open-core, коробка = свои секреты; pull избегает SSH-секретов в Actions |
| 69 | Phase 4: TLS брокера — **Caddy reverse-proxy с авто-Let's Encrypt**; брокер слушает `127.0.0.1`, наружу только Caddy (wss://). coturn на хосте, `static-auth-secret` == брокерский `--turn-secret` | авто-TLS без ручного обновления сертов; брокер не светит порт; coturn любит host-network |
| 70 | Phase 4: **серверная проверка чека Apple отложена** — подписки остаются client-asserted (#62), флип `source` без смены схемы возможен позже. Эта фаза = только дистрибуция + self-host | held-back по запросу: фокус фазы на доставке, а не на биллинге; схема уже готова к усилению |
| 71 | UX: при нажатии `P` рядом с pairing-URL **рисуем QR прямо в терминале** (`github.com/mdp/qrterminal/v3`, half-блоки, EC level L, quiet-zone 1) — навёл камеру iPad и открыл, как у Expo; URL остаётся как фолбэк. Вывод оборачиваем в CRLF-writer, т.к. `watchKeys` держит TTY в raw-mode | URL длинный (signal WS + base64-pubkey + одноразовый secret ≈ 200 симв.), копипаст на iPad больно; низкий EC оправдан — секрет одноразовый и окно короткое, плотность важнее коррекции |
| 72 | UX: **в бинарь запекается только адрес брокера** (`defaultSignalURL` через `-ldflags`, пусто в репо → unbaked = local-only; релиз и `make run-remote` подставляют `wss://simcast.sidorov.tech/ws`). `simcastd serve` без флагов сам коннектится к брокеру. **Никакой хостинг клиента/Caddy-статики не нужен**: pairing-URL самодостаточен, iOS читает его из QR; для веб-дебага страница берётся с `http://localhost:8080/` (`--web`, secure context) | прямая просьба «одна команда без параметров»; сервер участвует только как рандеву-брокер, не как раздача страницы |
| 73 | UX пейринга в терминале: клавиша **C** отменяет открытое окно (`win.Close()` чистит секрет, идемпотентно); при **отмене / протухании по TTL / успешном пейринге** блок (QR+URL) перерисовывается на месте — курсор поднимается на N строк (`\x1b[NA`), блок печатается faint (бледно-серый), снизу цветной статус (красный CANCELLED / серый EXPIRED / зелёный PAIRED). `pairUI` под мьютексом, повторный `retire` гейтится (гонки cancel/expire/enroll рисуют один раз) | видно, что код «погашен», а не висит живым; best-effort — ломается если блок проскроллил или между печатью и retire влез другой вывод |
| R1 | **Исследование латентности видео-пайплайна** (не решение, а замеры) — провели профилирование текущего PNG→ffmpeg→WebRTC пути: glass-to-glass ~0.6–0.7с, разбор по стадиям, эксперимент с `-flags low_delay`, факты из исходников idb/ffmpeg. Все цифры и шаги — в `docs/research/2026-06-08-latency-pipeline.md`. Измерительные правки кода откачены | чтобы при будущей оптимизации опираться на готовые замеры и не повторять пройденные шаги; выводов сознательно не фиксируем |
| 74 | **Lifecycle (list/boot) уходит с idb_companion на `xcrun simctl`.** `companion.List` = `simctl list -j devices available` (парс runtime-keyed JSON → `[]Simulator`, фильтр `isAvailable`, детерминированная сортировка); `companion.Boot` = `simctl boot <udid>`, идемпотентность через распознавание `current state: Booted` (как делал idb `--verify-booted`). idb_companion остаётся обязательным **только для стриминга** (gRPC describe/screenshot/hid, `internal/idb`); `Resolve`/`Version` сохранены как проверка его наличия. `simcastd list` больше не падает без idb — печатает пометку и всё равно перечисляет симуляторы. Тип `Simulator` и публичный API пакета не менялись (фронтенд/сервер не тронуты) | enumerate+boot — это нативная работа Apple-тулчейна, гонять её через сторонний бинарь незачем; снимает половину CLI-зависимости от idb и делает `list` рабочим даже до установки idb. Поля `Simulator` берём из simctl (`os_version` из runtime-id, `model` из deviceType); `architecture` никто не читает (проверено grep'ом) |
| 75 | **Удаление незащищённых транспортов.** Выпилены `WS /session` (JPEG-поллинг Phase 1: `session.go`, `framebuf.go`) и dev `WS /rtc` (`handleRTC` + `sdpMsg` в `rtc.go`) вместе с роутами в `mux`; в web-дебаге убраны JPG-режим, переключатель RTC/JPG и мёртвый `signalLocal`. Остаётся **единственный** медиапуть — аутентифицированный WebRTC через брокер (`ServeSignal`/`startSession`, за Ed25519-гейтом `authed`, #58). `startSession`/`rtcFPS` сохранены (общие внутренности remote-пути, не точка входа). Также удалены REST `/api/{simulators,boot}`: web-клиент их не дёргает, в remote `list`/`boot` идут по control-DataChannel (`rtcDispatch`) — HTTP-сервер демона теперь раздаёт только `--web` статику. Интерфейс `Companion`/поле `comp` сохранены (их юзает `rtcDispatch`). **Пересматривает #50** (JPG `/session` больше не «не трогаем») | все эндпоинты отдавали видео/инжект ввода/lifecycle без какой-либо аутентификации любому, кто дотянулся до порта (`:8080` на всех интерфейсах); по требованию пользователя оставляем только спаренный доступ. JPEG-MVP своё отслужил (Phase 1→2), dev `/rtc` дублировал remote-механику без auth, REST дублировал DataChannel-команды |
| 76 | **Live presence демонов** — отдельный WS-канал `watch`/`presence` на брокере, не связанный с `join`: клиент шлёт список `daemonID`, брокер отдаёт снапшот `states:map[daemonID]bool` и одно-ключевые дельты при регистрации/отвале демона. «Демон онлайн» ≡ «его WS к брокеру жив» (`b.daemons`) — источник правды уже есть, выставляем наружу. `watchers` под **тем же** `b.mu`, что и `daemons`; снапшот считается под `b.mu`, но **сериализуется через `c.wmu`, взятый до отпускания `b.mu`** (запись — уже без глобального лока, с write-deadline) — это строго упорядочивает снапшот перед любой дельтой `notifyPresence` (её запись блокируется на том же `c.wmu`), закрывая и потерю дельты, и переворот «дельта обогнала снапшот» (усиление спека, где это покрывалось лишь «идемпотентным мёржем»), **не удерживая `b.mu` на время сетевой записи** (иначе один зависший watcher морозил бы register/drop/join всего брокера). Keepalive ping/pong на conn'ах демонов (10s ping / 25s read-deadline) ловит half-open TCP при жёстком сне Mac; чистый выход рвёт WS и детектится мгновенно. **Демон не меняем** (gorilla авто-pong в `ReadJSON`-цикле `serveOnce`). Доступ по `daemonID` (публичный ключ), без отдельной auth — совпадает с моделью untrusted-брокера (#55). Веб-дебаг = референс-потребитель (🟢/⚪/?) | живой индикатор online/offline без тяжёлого `join`-рукопожатия; presence де-факто уже был виден только в момент полной попытки стрима — вынесли в лёгкий канал |
| 77 | **Switch Off (shutdown) добавлен в control-протокол.** Демон принимает `{"type":"shutdown","udid":…}` по control-DataChannel → `companion.Shutdown` = `simctl shutdown <udid>` (идемпотентность через распознавание `current state: Shutdown`, как Boot), отвечает `{"type":"shutdown","udid":…}` или `error`. Если выключаемый симулятор — это текущий стрим (`attachment.udid`), фид сначала останавливается (`stopAttachment`); фид другого симулятора не трогаем | закрывает BLIND-SPOTS #1: до этого Switch Off в iOS был чисто оптимистичным (UI рисовал «Shut Down», симулятор на Mac оставался Booted, pull-to-refresh возвращал реальное состояние) |
| 78 | **Mac имя + версия macOS отдаются по control-каналу (hello), не в pairing-URL.** На открытие control-DataChannel демон шлёт несолиситед `{"type":"hello","name":…,"osVersion":…}` (`rtc.Session.OnControlOpen` → `rtcDispatch.sendHello`). Имя = `scutil --get ComputerName`, версия = `sw_vers -productVersion` (best-effort в `cmd/simcastd`, пусто → клиент прячет сабтайтл), прокидываются через `Server.WithHost`. **Выбран hello, а не URL** | закрывает BLIND-SPOTS #2 (имя было плейсхолдером `Mac <8 симв. daemonID>`, версия всегда nil). hello долетает до КАЖДОГО коннекта (в т.ч. до уже спейренных Mac'ов и после апдейта ОС остаётся свежим), тогда как URL заморозил бы значение на момент скана и не дошёл бы до ранее спейренных |
| 79 | **Явный pin-ack: hello несёт `paired:true`.** Демон пиннит ключ (`pinned.Add`) ДО `authed=true`, и теперь **проверяет ошибку Add** — при провале шлёт `error` + `cleanup()`, не аутентифицирует. Раз дойти до control-канала можно только за auth-гейтом, сам факт прихода hello = доказательство, что ключ сохранён; кладём это в `paired:true`. Клиент, сохранивший Mac оптимистично на скане, подтверждает пейринг по этому ack (дозвон, оборвавшийся до hello → пин не подтверждён, можно перепейрить) | закрывает BLIND-SPOTS #3: раньше Add-ошибка глоталась (`_ = pinned.Add`), и Mac мог оказаться «сохранён на iPad, но не запинен на демоне» → вечно offline на реконнекте (challenge fail). Отдельное сообщение не вводим — hello уже шлётся на открытие канала (#78), складываем device-info и pin-ack в один greeting |
| 81 | **Shake добавлен в control-протокол — через Darwin-нотификацию, не через idb HID.** Демон принимает `{"type":"shake"}` по control-DataChannel и трясёт **текущий приаттаченный** симулятор (udid берём из `attachment`, как у `home`, а не с провода). Реализация — `companion.Shake` = `simctl spawn <udid> notifyutil -p com.apple.UIKit.SimulatorShake`: у simctl нет сабкоманды shake, а idb HID моделирует только touch/button/swipe (в `idb.proto` кнопки APPLE_PAY/HOME/LOCK/SIDE_BUTTON/SIRI, shake нет), поэтому оба привычных пути ввода до тряски не дотягиваются. `com.apple.UIKit.SimulatorShake` — ровно та нотификация, что постит Simulator.app для Device ▸ Shake (найдена в строках его бинаря); `spawn` доставляет её в домен симулятора headless — без окна Simulator.app и без приватного CoreSimulator-шима. Shake **fire-and-forget**, как tap/home/swipe/key: бьёт по приаттаченному симу, нет аттача → тихий no-op, ошибка → только `log`, **ответа нет** (по решению iOS-команды). Изначально отвечал `{"type":"shake"}`/`error`, но `error` уводил UI клиента в «disconnected», а success-udid iOS не нужен — консистентность с братскими жестами важнее | iOS-команда просила shake «как home», но наш home — это idb HID, где тряски нет; путь через AppleScript-меню Simulator.app невозможен (симы гоняем headless, окна нет), а приватный CoreSimulator-шим нарушил бы правило «не пишем свой CoreSimulator-пайплайн». Darwin-нотификация через публичный `simctl spawn` — единственный headless-путь, укладывающийся в «тонкая обёртка над CLI» |
| 80 | **Типизированный код ошибки вместо эвристики по тексту.** В `signal.Msg` добавлено поле `Code` + константы `offline / pair_expired / pair_used / pair_invalid`. `pairingWindow.verify` теперь возвращает не `bool`, а `pairResult` (`pairOK/pairNoWindow/pairExpired/pairUsed/pairBadProof`) — для этого окно хранит `consumed`+`cancelled` и **не затирает `expires`** на закрытии, чтобы отличить «протухло» от «уже использовано». Демон мапит причину в `Code` рядом с прежним `Msg:"not paired"`; брокер тегает offline-отказ `CodeOffline`. `Msg` остаётся человекочитаемым | закрывает BLIND-SPOTS #4: iOS различал «expired / already-used» по подстроке «pair» в тексте ошибки (`reason.localizedCaseInsensitiveContains("pair")`) — хрупко. Теперь клиент ветвится по стабильному коду; текст можно менять, не ломая UI |
| 82 | **Стримимое устройство абстрагировано интерфейсами `server.Backend`/`server.Feed`.** `Backend` = `Companion` + `Attach(udid) → Feed`; `Feed` = `Screen`/`Frames` (готовые H.264 access units)/`Input` (нормализованные [0,1] координаты + KeyboardEvent.key)/`Close`. Вся idb-механика (сайдкар, describe, скриншот→ffmpeg, HID, keymap) переехала в `internal/backend/sim`; сессионный слой (`rtcDispatch`) и рандеву/пейринг бэкенда не знают. Выбор бэкенда — рантаймовый (сабкоманда), без build tags: всё компилируется под любую ОС | нужен демо-endpoint без Mac (App Review); срез ровно один — `attach`+`input`, остальное уже было за интерфейсом `Companion`. Бонус: `rtcDispatch` тестируется фейковым `Feed` без ffmpeg/idb |
| 83 | **Кодек ffmpeg выбирается по GOOS**: darwin → `h264_videotoolbox` (`-realtime 1`), остальные → `libx264` (`-preset ultrafast -tune zerolatency`). Общий контракт пайплайна (image2pipe PNG, короткий GOP, baseline, raw h264 в stdout) неизменен | Linux-демону не на что рассчитывать аппаратно; x264 ultrafast на демо-кадре (халвинг №40) дёшев. Один пакет `encoder` на оба бэкенда |
| 84 | **Демо-бэкенд — headless Chromium через chromedp (`internal/backend/browser`)**: mobile-эмуляция (390×844 CSS, DPR 2, touch), скриншот-полл 15fps в тот же encoder, tap/swipe → CDP `dispatchTouchEvent` (свайп размазан по duration, чтобы Chromium синтезировал скролл/флинг), key → CDP keyboard, Home → навигация на стартовый URL. Один Chromium на attach. Единственное всегда-Booted устройство `demo`; Boot/Shutdown/Shake — спокойные no-op | полная интерактивность для ревьюера/демо дешевле и убедительнее канированного видео; модель «скриншот-полл → ffmpeg» идентична sim-бэкенду (№34–№40), другой только источник PNG. Реальные фреймворки (idb) на Linux недоступны — скоуп «только симуляторы» не нарушен: это не поддержка устройств, а демо-заглушка протокола |
| 85 | **`simcastd demo`: unattended multi-use pairing.** Окно энроллмента взводится на старте фиксированным секретом (`--pair-secret`/`$SIMCAST_PAIR_SECRET`, иначе генерится) с TTL ~100 лет и **перевзводится в `OnEnroll`** после каждого успешного пейринга — один напечатанный pairing URL обслуживает любое число клиентов и переживает рестарты. Стейт по умолчанию `~/.simcast/demo-*` (не конфликтует с serve на том же Mac). Дистрибуция: GoReleaser собирает `simcastd` и под linux amd64/arm64 (отдельный archive id — cask видит только darwin), деплой — `deploy/systemd/simcastd-demo.service` + `demo.env.example`, deps на Linux: `apt install chromium ffmpeg` | сознательный размен single-use анти-абьюза (№58) на автономность: демо-бокс одноразовый и ничего ценного не стримит, а ревьюеру Apple нужен стабильный URL в notes без человека у терминала |
| 86 | **Reconnect-лог по умолчанию — только переходы, детали за `-v`.** Раньше `ServeSignal` печатал строку на КАЖДУЮ попытку реконнекта (`signaling connection lost: …; reconnecting in Ns`), и циклы сна/пробуждения Mac забивали консоль (одна строка на пробуждение), да ещё лесенкой из-за raw-mode TTY (см. фикс OPOST). Вынес нарратив в тестируемый `reconnLog{logf,verbose,down}`: `serveOnce` после успешного register дёргает `onUp()`, дефолт печатает одну строку `broker connection lost (up 14m) — reconnecting` на переходе healthy→down и `broker back online` на восстановлении, ретраи между ними молчат. Флаг `-v` (на `serve` и `demo`, через `Server.WithVerbose`) возвращает прежний per-attempt лог с причиной | смысл сообщения — сигнал «ты сейчас офлайн / снова онлайн», а не поток технических причин; полностью прятать под `-v` нельзя (при реальном обрыве нужен хоть какой-то сигнал по умолчанию), поэтому дефолт = две строки на обрыв, а причина+backoff уходят в verbose |
| 87 | **Полноразрешённый скриншот — отдельный надёжный DataChannel `bulk`.** Клиент (offerer) создаёт упорядоченный надёжный канал `bulk` рядом с `control`/видео; демон роутит его по метке в `OnDataChannel` (`rtc.Session`, `SendBulk`=бинарь/`SendBulkText`=текст). Запрос `{"type":"screenshot"}` → снимок **текущего приаттаченного** фида (`Feed.Screenshot(ctx)`: sim = gRPC `Screenshot` полного ретина-разрешения, browser = chromedp `CaptureScreenshot`). Ответ-успех — **текстовый заголовок** `{"type":"screenshot","bytes":N}`, за ним **бинарные чанки**, конкатенация которых даёт ровно N байт PNG; ответ-ошибка — **текстовый** конверт `{"type":"error","msg":…}`. Клиент различает успех/ошибку по binary/text-флагу фрейма. Чанкинг не опционален: SCTP жёстко режет `Send` больше согласованного пиром `a=max-message-size` (libwebrtc отдаёт 256 KiB, pion при отсутствии атрибута падает на 65535), а полноразмерный PNG — мегабайты; размер чанка берём из `Session.MaxMessageSize()` (реально согласованное), клампим потолком 200 KiB, при неизвестном капе — 16 KiB. Порядковые номера не нужны — канал reliable+ordered, клиент просто аппендит до объявленного N. Демон отвечает всегда: нет аттача / ошибка захвата / пустой захват / неизвестный тип / битый JSON → текстовая ошибка; захват под таймаутом ~15с, чтобы опередить клиентский ~20с failsafe. `handleBulk` крутится на своей per-channel goroutine pion (не блокирует control), корреляция по id не нужна — клиент держит один запрос в полёте | контракт задан iOS-стороной (`broadcaster-screenshot-protocol.md` в репо клиента, не здесь): кнопка «скриншот» должна сохранять кадр в полном разрешении устройства, а видеопоток заведомо half-res (№40 халвинг) и лоссовый H.264. `control` ненадёжный (`maxRetransmits=0`) — многомегабайтный PNG по нему терять нельзя; сигналинг закрывается после P2P и untrusted (№55) — гнать через брокер содержимое экрана нельзя. Отсюда третий канал: надёжный, упорядоченный, вне data-path брокера |
| 88 | **Качество видео — клиентская ручка: `scale` + `bitrate`, без пресетов на демоне.** Провод несёт числа (`scale` 0.25…1.0 — множитель разрешения источника; `bitrate` 500k…16M бит/с), пресеты живут в UI iPad-клиента. Дефолты воспроизводят прежнее захардкоженное поведение: sim `0.5`/`8M` (халвинг №40), browser `1.0`/`8M`. Ноль/отсутствие поля → дефолт бэкенда, значение вне диапазона → кламп, не ошибка. Стартовое качество едет в `attach` (control), смена на лету — **`{"type":"quality",…}` по `bulk`** (reliable+ordered, №87), ответ — эхо применённого после клампа. `control` для этого не годится: `maxRetransmits: 0` теряет сообщение ровно на той плохой сети, ради которой ручка и нужна. Смена = внутренний re-attach (argv ffmpeg фиксируется на спавне), уходит в горутину — `handleBulk` блокировать нельзя, там `doScreenshot` с 15с таймаутом; трек не пересогласовывается, стартовый IDR ресинкает декодер (№50). `Backend` получил `Attach(ctx, udid, QualityOpts)` + `DefaultScale()` (дефолт — знание бэкенда, а сессия обязана назвать клиенту применённое); `encoder.Encode`/`EncodeFullRes` схлопнуты в одну функцию с параметром. **`fps` сознательно не ручка**: idb `Screenshot` RPC стоит 71.5–73.9 мс против 66.67 мс интервала поллинга (`docs/research/2026-06-08-latency-pipeline.md`) — захват уже потолок, ручка обещала бы невозможное. Адаптивный битрейт по-прежнему отложен (ARCHITECTURE) — это ручной выбор. Дизайн — `docs/superpowers/specs/2026-07-14-video-quality-design.md` | «хочу дать пользователям указывать желаемые настройки, сети могут быть разные». Пресеты на клиенте (а не на демоне) — осознанный размен: перенастройка цифр теперь требует App Review, зато клиент волен показывать любой UI, а демон остаётся владельцем допустимого диапазона через кламп |
| 88a | **Фильтр масштаба — `scale=trunc(iw*S/2)*2:-2`, чётность явная.** scale-фильтр ffmpeg сам не округляет до чётного: наивный `scale=iw*S:ih*S` при нечётном результате молча чинится в `h264_videotoolbox`, но `libx264` жёстко отказывается стартовать (`width not divisible by 2 (1061x2300)` при S=0.9 на 1179×2556). При `S == 1.0` фильтр не добавляется вовсе (источник уже в целевом разрешении — незачем платить за проход скейлера) | прежний захардкоженный `scale=iw/2:ih/2` жил только на darwin (sim), а browser не фильтровал вообще — поэтому баг спал. Первая же клиентская ручка `scale` уронила бы Linux-демо на старте энкодера. Проверено прогоном обоих энкодеров |
| 89 | **Bulk-ошибки получили стабильный `code`** (`unknown_type` / `bad_request` / `no_attachment` / `capture_failed`) рядом с человекочитаемым `msg` — тот же контракт, что у `signal.Msg` (№80). Ключевое здесь не гигиена, а **версионный щуп**: клиент не может спросить у `attach`, понимает ли демон `scale`/`bitrate` (старый молча глотает незнакомые поля JSON и аттачит на своих числах), поэтому щупает `{"type":"quality"}` по bulk **до аттача** — новый демон отвечает `no_attachment`, старый `unknown_type`, и оба ответа бесплатны (`doQuality` при `att == nil` выходит до спавна). Щуп после аттача тоже сработал бы, но стоил бы пересборки фида (~1.5с) | различать «поддерживает» и «не поддерживает» по подстроке `unknown bulk type` — ровно та хрупкость, от которой ушли в №80. Всплыло из вопроса: клиент без лайв-изменения качества всё равно обязан знать, применился ли его `scale` |
| 89a | **Замер стоимости смены качества: ~1.5с, из них ~1.2с — респавн `idb_companion`.** `doQuality` → `doAttach` → `backend.Attach` → `idb.Spawn`, который поднимает новый процесс сайдкара и поллит `Describe` каждые 300мс до готовности (три прогона: 1.225 / 1.215 / 1.214с). Сайдкару `scale`/`bitrate` безразличны — он лишь источник PNG, — так что этот 1.2с платится впустую. **Не оптимизируем пока**: чинится либо стабильным каналом `Feed.Frames()` с форвардингом внутри фида (сейчас `attach.go` делает `for f := range feed.Frames()`, подмена канала убьёт насос), либо кешем сайдкаров по udid. Вместо этого зафиксировано в протоколе: стартовое качество класть в `attach` (бесплатно, один спавн), `attach`→`quality` — антипаттерн (+1.5с на каждый старт), лайв-смену коммитить на отпускании ползунка | цифра нужна клиенту для UX-решений (спиннер? дебаунс?), а «well under a second» было выдумкой автора — поймано при вычитке дока и померено. Оптимизация не входила в утверждённый скоуп; клиент без лайв-смены её и не заметит |
